# Plan: Correction erreur "schema[~standard].validate" sur la page Scène

## Problème
Erreur JavaScript lors de l'enregistrement d'une scène:
```
undefined is not an object (evaluating 'schema["~standard"].validate')
```

## Analyse Root Cause

L'erreur se produit dans la configuration du formulaire avec React Hook Form + Zod. Le problème principal est l'utilisation de `useMemo` pour calculer les `defaultValues` qui peut retourner des valeurs `undefined` non gérées correctement par Zod.

### Lignes problématiques identifiées (scene.tsx):
- **Ligne 1**: `useEffect` manque dans les imports
- **Lignes 190-200**: Les `defaultValues` calculées avec `useMemo` peuvent causer des problèmes de synchronisation

## Solution

Utiliser `reset` avec `useEffect` pour initialiser le formulaire après le chargement des données, ce qui est le pattern recommandé pour les données asynchrones.

## Changements nécessaires

### 1. Ligne 1: Ajouter `useEffect` aux imports
```typescript
import { useMemo, useState, useCallback, useEffect } from 'react';
```

### 2. Lignes 190-220: Remplacer l'initialisation du formulaire

**AVANT:**
```typescript
const { register, handleSubmit, control, watch, formState: { errors, isDirty } } = useForm<SceneInput>({
  resolver: zodResolver(sceneSchema),
  defaultValues: useMemo(() => ({
    title: scene?.title || '',
    summary: scene?.summary || '',
    content: scene?.content || '',
    characters_json: toCsv(scene?.charactersJson),
    tags_json: toCsv(scene?.tagsJson),
    notes: scene?.notes || '',
  }), [scene]),
});
```

**APRÈS:**
```typescript
const { register, handleSubmit, control, watch, reset, formState: { errors, isDirty } } = useForm<SceneInput>({
  resolver: zodResolver(sceneSchema),
  defaultValues: {
    title: '',
    summary: '',
    content: '',
    characters_json: '',
    tags_json: '',
    notes: '',
  },
});

// Reset form when scene data loads
useEffect(() => {
  if (scene) {
    reset({
      title: scene.title,
      summary: scene.summary ?? '',
      content: scene.content ?? '',
      characters_json: toCsv(scene.charactersJson) ?? '',
      tags_json: toCsv(scene.tagsJson) ?? '',
      notes: scene.notes ?? '',
    });
  }
}, [scene, reset]);
```

## Fichier concerné
`/home/ergen35/Sources/repos/creative-work/kuti-studio-system/kuti-frontend/app/routes/scene.tsx`

## Validation
Après les modifications:
1. La page doit charger sans erreur
2. Les données de la scène doivent s'afficher correctement dans le formulaire
3. L'enregistrement doit fonctionner sans l'erreur `schema["~standard"].validate`
